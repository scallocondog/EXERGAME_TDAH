{
  "name": "movimente-controller",
  "version": "0.1.0",
  "private": true,
  "description": "Página web del mando de MoviMente (la sirve server/)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test"
  }
}
